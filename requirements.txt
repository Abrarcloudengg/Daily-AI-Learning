# Runtime dependencies.
#
# Kept in sync with [project.dependencies] in pyproject.toml. `pip install -e .`
# is the preferred install; this file exists so that `python scripts/generator.py`
# keeps working on a checkout where the package was never installed.

requests>=2.31
python-dotenv>=1.0
